# Unity 프로젝트 작업 지침

## 프로젝트 개요

- 이 저장소는 **OpenAI 게임 빌더스 서울 2026**에서 선보일 게임의 소스 저장소이다.
- 프로젝트는 **Unity 6.3 LTS**를 사용한다.
- 게임은 2D로 제작한다.
- 최종 배포 대상은 별도 설치 없이 브라우저에서 바로 실행되는 Unity 웹 빌드이다.
- 프로젝트 문서는 형제 저장소 `../openai-game-builders-seoul-2026.wiki`에서 관리한다.

## 기본 작업 원칙

- 작업 전에 현재 프로젝트 구조, `ProjectSettings/ProjectVersion.txt`, `Packages/manifest.json`과 기존 코드 관례를 확인한다.
- 작업 시작 시 `unity status`로 실행 중인 Unity Editor 연결 여부를 확인한다.
- Unity 버전, 렌더 파이프라인, 입력 시스템 또는 패키지를 임의로 변경하지 않는다.
- 새 패키지나 외부 에셋을 추가하기 전에 필요성, 라이선스 및 웹 빌드 호환성을 확인한다.
- 사용자가 요청한 범위만 수정하고 관련 없는 변경은 보존한다.
- 커밋, 푸시, 배포 또는 원격 서비스 변경은 명시적으로 요청받은 경우에만 수행한다.

## 2D 게임 기준

- 게임플레이 구현에는 Unity의 2D 기능과 물리 시스템을 우선 사용한다.
- 3D 에셋, 3D 물리 또는 불필요한 3D 렌더링 기능은 명확한 이유가 있을 때만 도입한다.
- 스프라이트의 Pixels Per Unit, 피벗, 필터 모드, 압축 설정과 정렬 레이어의 일관성을 유지한다.
- 월드 공간, 화면 공간, 픽셀 좌표를 혼용할 때 변환 책임을 명확히 한다.
- 해상도와 화면 비율이 달라도 UI와 주요 게임플레이 영역이 올바르게 표시되도록 설계한다.

## 브라우저 웹 빌드 기준

- 파일 시스템, 스레딩, 네이티브 플러그인, 네트워크 및 브라우저 보안 제약을 고려한다.
- 웹 빌드에서 지원되지 않거나 동작이 다른 API를 사용하기 전에 호환성을 확인한다.
- 초기 다운로드 크기, 메모리 사용량, 로딩 시간과 런타임 성능을 고려한다.
- 키보드, 마우스와 브라우저 포커스 변화에 따른 입력 동작을 점검한다.
- 플랫폼별 코드는 전처리 지시문과 명확한 대체 동작으로 격리한다.

## Unity 자산과 직렬화 파일

- `Assets/` 안의 자산을 추가, 이동 또는 삭제할 때 대응하는 `.meta` 파일도 함께 관리한다.
- 기존 `.meta` 파일의 GUID를 임의로 바꾸거나 재생성하지 않는다.
- `.unity`, `.prefab`, `.asset` 등 Unity YAML 파일은 가능하면 Unity Editor 또는 검증된 Editor 스크립트를 통해 수정한다.
- 씬·프리팹·에셋 작업 전 `unity status`로 연결된 Editor를 확인하고, 있으면 `unity-cli` 스킬을 로드해 live 조작한다. 연결된 Editor가 없을 때만 파일 직접 편집으로 폴백한다.
- 직렬화 파일을 텍스트로 직접 수정해야 한다면 변경 범위를 최소화하고 파일 ID, GUID 및 참조가 보존되는지 확인한다.
- `Library/`, `Temp/`, `Obj/`, `Logs/`, `UserSettings/`, 빌드 출력물과 IDE 생성 파일을 커밋하지 않는다.
- `.gitignore`와 `.gitattributes`의 기존 Unity 및 줄바꿈 정책을 유지한다.

## C# 코드

- 기존 어셈블리, 네임스페이스, 폴더 구조와 명명 규칙을 따른다.
- `MonoBehaviour`는 수명 주기와 Unity 객체 연결에 집중시키고, 가능한 게임 로직은 테스트 가능한 일반 C# 클래스로 분리한다.
- 프레임마다 호출되는 코드에서 불필요한 할당, 반복 검색과 무거운 작업을 피한다.
- Inspector 참조는 의도를 명확히 하고 누락 시 이해할 수 있는 오류를 제공한다.
- 공개 API 또는 직렬화 필드의 이름을 변경할 때 기존 씬과 프리팹 데이터의 호환성을 확인한다.
- 단순한 문제에 불필요한 추상화나 전역 싱글턴을 도입하지 않는다.

## 테스트와 검증

- 변경 후 컴파일 오류와 Unity Console의 새 오류 또는 경고를 확인한다.
- 로직 변경에는 가능하면 Unity Test Framework의 Edit Mode 또는 Play Mode 테스트를 추가하거나 갱신한다.
- 관련 테스트가 있으면 실행하고, 실행할 수 없다면 그 이유와 수동 검증 방법을 보고한다.
- 테스트는 `unity test`, 웹 빌드는 `unity build --target WebGL` 등 Unity CLI를 우선 사용하고, `unity logs --follow`로 Console 에러를 확인한다.
- 씬, 프리팹 또는 자산을 변경했다면 참조 누락과 직렬화 손상을 확인한다.
- 사용자에게 보이는 변경은 서로 다른 화면 비율에서 확인한다.
- 웹 관련 변경은 브라우저 웹 빌드에서 로딩, 입력, 오디오, 저장 및 네트워크 동작 중 관련 항목을 검증한다.

## 문서 연계

- 구현이나 사용자 흐름이 바뀌면 위키 문서의 갱신 필요성을 확인한다.
- 소스 코드 변경만 요청받았다면 위키 저장소는 참고할 수 있지만 명시적 요청 없이 수정하지 않는다.
- 기술 문서에 버전, 패키지, 설정값 또는 경로를 기록할 때 저장소의 실제 상태와 일치시킨다.

## 완료 보고

- 변경한 코드, 자산 및 설정을 간결하게 요약한다.
- 수행한 테스트와 확인하지 못한 항목을 구분해 보고한다.
- 잠재적인 웹 빌드 제약이나 후속 작업이 있으면 명시한다.
